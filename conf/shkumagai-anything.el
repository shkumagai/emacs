;; Anything
(require 'anything-startup)

;; ;; Anything etags+
;; (require 'anything-etags+)
;; (setq anything-etags+-use-short-file-name nil)
;; (setq anything-etaga+-highlight-tag-after-jump t)
;; (global-set-key (kbd "M-.") 'anything-etags+-select-one-key)
;; (global-set-key (kbd "M-*") 'anything-etags+-history)
;; (setq tags-table-list '("~/refs/TAGS"))
